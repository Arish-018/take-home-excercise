<% flash.each do |key, value| %>
  <div class ="alert alert-<%=key == 'notice' ? 'success' : 'danger' %>">
    <% if value.is_a?(Array)%>
      <%value.each do |v|%>
          <li><%=v%></li>
      <%end%>
      <%else%>
        <li><%=value%></li>
      <%end%>
  </div>
<% end %>
